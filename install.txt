https://winscp.net/eng/downloads.php
https://www.chiark.greenend.org.uk/~sgtatham/putty/latest.html
"C:\Program Files\PuTTY\putty.exe" -ssh root@77.37.49.7 -pw "PASSWORD"

# change domain name

# copy mytoken.space.zip

sudo apt-get install unzip -y
unzip mytoken.space.zip

sudo apt update
sudo apt install default-jre

sudo ufw allow 21
sudo ufw allow 80
sudo ufw allow 443
sudo ufw allow 8843

# change start.sh remove master
chmod +x /root/start.sh
/root/start.sh

cp mfm.service /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable mfm.service

sudo systemctl restart mfm

# change start.sh add master

sudo apt-get update
sudo apt-get install certbot -y
// для одного домена
sudo certbot certonly --standalone -d vavilon.org --force-renewal

// генерация keystore
openssl pkcs12 -export -in /etc/letsencrypt/live/[vavilon.org-0001]/fullchain.pem -inkey /etc/letsencrypt/live/[vavilon.org-0001]/privkey.pem -out keystore.p12 -name jetty -password pass:x29a100
keytool -importkeystore -srckeystore keystore.p12 -srcstoretype pkcs12 -srcstorepass x29a100 -destkeystore keystore.jks -deststoretype JKS -deststorepass x29a100


// для поддоменов с добавлением txt записи в dns
// certbot certonly --manual --preferred-challenges=dns -d "*.vavilon.org" -d "vavilon.org" --agree-tos --manual-public-ip-logging-ok --email your-email@example.com


# отключить системные логи а то переполняет постоянную память
в файле /etc/logrotate.d/rsyslog

/var/log/syslog
/var/log/mail.log
/var/log/kern.log
/var/log/auth.log
/var/log/user.log
/var/log/cron.log
{
    su root syslog
    rotate 7
    daily
    maxsize 1M
    missingok
    notifempty
    compress
    delaycompress
    sharedscripts
    postrotate
        /usr/lib/rsyslog/rsyslog-rotate
    endscript
}

# поиск больших файлов
find / -type f -size +100M -exec du -ah {} + | sort -rh


решение:
/etc/rsyslog.d/50-default.conf
-
*.*;auth,authpriv.none -/var/log/syslog
+
*.*;auth,authpriv.none;cron.none -/var/log/syslog

service rsyslog restart




/etc/logrotate.conf
weekly -> dayly